version: "3.8"

services:
  order-service:
    build: .
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      PORT: ${PORT}
      DATABASE_URL: ${DATABASE_URL}
      ORDER_URL: ${ORDER_URL}
      DELIVERY_URL: ${DELIVERY_URL}
      NOTIFICATION_URL: ${NOTIFICATION_URL}
